/**
 * When using a custom font-face, this property inside the typography
 * object is set to a mapping of font styles and the desired font families.
 *
 * For example,
 *
 * ```
 * {
 *   "regular": "Obsidian-Reg, sans-serif",
 *   "bold": "Obsidian-Bold, sans-serif"
 * }
 * ```
 *
 * Components will automatically use keys for "regular", "bold" and "medium"
 * if they have been set. Custom font styles, such as "extraBold" can be used like:
 *
 * <Text weight="extraBold" ... />
 */
const font = null;

export default font;
